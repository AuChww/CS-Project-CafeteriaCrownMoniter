class UserEntity:
    def __init__(self, user_id, user_image, username, password, email, role):
        self.user_id = user_id
        self.user_image = user_image
        self.username = username
        self.password = password
        self.email = email
        self.role = role
